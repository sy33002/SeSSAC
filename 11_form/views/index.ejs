<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>폼</title>
</head>
<body>
    <h1><%= title %></h1>
    <h4>GET 요청</h4>
    <form action="/getForm" method="get">
        <input 
        type="text" 
        name="id" 
        placeholder="ID는 최소 3글자" 
        required 
        minlength="3">
        
        <input type="text" name="pw" id="" placeholder="PW는 최소 4글자" required minlength="4"><br/>
        <input type="email" name="email"  placeholder="email"/>
        <input type="number" name="grade" class="number" placeholder="grade"><br/>
        <button>제출</button>
    </form>
    
    <h4>POST 요청</h4>
    <!-- required 속성: 폼 요소에 값이 반드시 들어가야하는지 여부를 결정 -->
    <!-- pattern 속성: 폼 요소에 값 입력되었을 때 그 입력된 값에 대한 검증을 의미 -->
    <form action="/postForm" method="post">
        <input type="text" 
        name="id" placeholder="ID" 
        pattern="^([a-zA-Z0-9가-힣]){4,}$" 
        title="아이디는 대소문자, 한글, 숫자로 구성된 4자 이상의 문자여야 합니다.">
        <!-- title: 말풍선에 말을 적을 수 있음 -->
        <input type="password" 
        name="pw" placeholder="PW" pattern="^([a-z0-9]){8,12}$" 
        title="비밀번호는 영어소문자와 숫자로 구성된 8자리 이상 12자리 이하의 문자여야 합니다."/><br>
        <button>제출</button>
    </form>
</body>
</html>
